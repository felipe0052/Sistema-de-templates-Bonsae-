{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "preDeployCommand": ["php artisan migrate --seed --force"],
    "restartPolicyType": "ON_FAILURE"
  }
}
